# Checkpoint - Aula 12

Responda no final da aula ou use como revisão antes de entregar a atividade.

## Perguntas

1. Para que serve o `__init__`?

2. O que representa o `self`?

3. O que é um método?

4. Qual é a diferença entre atributo e método?

5. O que acontece nesta linha?

```python
vida1 = VidaFinanceira("Ana", 2500, 1800)
```

6. O que este método retorna?

```python
def calcular_saldo(self):
    return self.renda - self.despesas
```

7. Por que usamos `self.renda` e `self.despesas` dentro do método?

---
5. Está sendo criado um objeto chamado `vida1` a partir da classe `VidaFinanceira`.

6. Retorna o saldo financeiro do objeto.

7. Porque `renda` e `despesas` são atributos guardados dentro do objeto.
